#include "Engine.hpp"
#include "Jolt.h"

int main()
{
    
    

    //RedFoxEngine::Engine engine(1920, 1040);

    /*
    while (engine.isRunning())
    {
        engine.ProcessInputs();
        engine.Update();
        engine.Draw();
    }
    */
    return (0);
}
